{
  "name": "backend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "./gradlew bootRun",
    "build": "./gradlew clean build",
    "dev": "./gradlew bootRun",
    "containers": "docker compose down && docker-compose up -d"
  },
  "dependencies": {
    "pnpm": "^6.14.0"
  }
}
